\input{preamble}
\input{macros}

% A quick way of writing the title using the predefined title in preamble.tex
\coursename{Name of the Course}
% Or uncomment this if you want to customise the whole title:
% \title{Title name}

\begin{document}
\maketitle

% A section of equations
\eqsection{Section Name One}{
    % Adding equations into a section
    \eq{Equation Name}{a^2 + b^2 = c^2}
    \eq{Equation Name}{a^2 + b^2 = c^2}
    \eq{Equation Name}{a^2 + b^2 = c^2}
    \eq{Equation Name}{a^2 + b^2 = c^2}
    \eq{Equation Name}{a^2 + b^2 = c^2}
    \eq{Equation Name}{a^2 + b^2 = c^2}
}

\eqsection{Section Name Two}{
    \eq{Equation Name}{a^2 + b^2 = c^2}
    \eq{Equation Name}{a^2 + b^2 = c^2}
    \eq{Equation Name}{a^2 + b^2 = c^2}
    \eq{Equation Name}{a^2 + b^2 = c^2}
    \eq{Equation Name}{a^2 + b^2 = c^2}
    \eq{Equation Name}{a^2 + b^2 = c^2}
}

\end{document}